[project]
name = "wbgapi_server"
version = "0.1.0"
description = "MCP Server for wbgapi_server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "#   mcp",
    "#   pandas",
    "#   structlog",
    "#   wbgapi",
    "mcp"
]

[tool.uv]
dev-dependencies = []
