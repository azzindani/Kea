[project]
name = "web3_server"
version = "0.1.0"
description = "MCP Server for web3_server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "eth-abi",
    "mcp",
    "pandas",
    "pydantic",
    "python-dotenv",
    "structlog",
    "web3",
    "wrapt",
]

[tool.uv]
dev-dependencies = []
