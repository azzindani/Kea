[project]name = "duckdb_server"version = "0.1.0"description = "MCP Server for duckdb_server"readme = "README.md"requires-python = ">=3.12"dependencies = [    "duckdb",    "mcp",    "pandas",    "pyarrow",    "pydantic",    "python-dotenv",    "structlog",    "wrapt",][tool.uv]dev-dependencies = []