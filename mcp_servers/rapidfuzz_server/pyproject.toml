[project]
name = "rapidfuzz_server"
version = "0.1.0"
description = "MCP Server for rapidfuzz_server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp",
    "pydantic",
    "python-dotenv",
    "rapidfuzz",
    "structlog",
    "wrapt",
]

[tool.uv]
dev-dependencies = []
