[project]name = "filesystem_server"version = "0.1.0"description = "MCP Server for filesystem_server"readme = "README.md"requires-python = ">=3.12"dependencies = [    "asyncpg",    "mcp",    "pydantic",    "python-dotenv",    "structlog",    "wrapt",][tool.uv]dev-dependencies = []