[project]
name = "zipfile_server"
version = "0.1.0"
description = "MCP Server for zipfile_server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp",
    "pydantic",
    "python-dotenv",
    "structlog",
    "wrapt",
]

[tool.uv]
dev-dependencies = []
