[project]
name = "geopy_server"
version = "0.1.0"
description = "MCP Server for geopy_server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "geopy",
    "mcp",
    "pydantic",
    "python-dotenv",
    "structlog",
    "wrapt",
]

[tool.uv]
dev-dependencies = []
