[project]
name = "spacy_server"
version = "0.1.0"
description = "MCP Server for spacy_server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp",
    "pydantic",
    "python-dotenv",
    "spacy",
    "structlog",
    "wrapt",
]

[tool.uv]
dev-dependencies = []
