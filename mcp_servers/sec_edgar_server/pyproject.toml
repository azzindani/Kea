[project]
name = "sec_edgar_server"
version = "0.1.0"
description = "MCP Server for SEC Edgar"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=0.1.0",
    "sec-edgar-downloader",
    "httpx",
    "beautifulsoup4",
    "wrapt",
    "pydantic",
    "structlog",
    "python-dotenv"
]

[tool.uv]
dev-dependencies = []
