# Kubernetes Services and Ingress

apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  labels:
    app: project
    component: api-gateway
spec:
  selector:
    app: project
    component: api-gateway
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: orchestrator
  labels:
    app: project
    component: orchestrator
spec:
  selector:
    app: project
    component: orchestrator
  ports:
    - port: 8001
      targetPort: 8001
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: mcp-host
  labels:
    app: project
    component: mcp-host
spec:
  selector:
    app: project
    component: mcp-host
  ports:
    - port: 8002
      targetPort: 8002
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: rag-service
  labels:
    app: project
    component: rag-service
spec:
  selector:
    app: project
    component: rag-service
  ports:
    - port: 8003
      targetPort: 8003
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: vault
  labels:
    app: project
    component: vault
spec:
  selector:
    app: project
    component: vault
  ports:
    - port: 8004
      targetPort: 8004
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: swarm-manager
  labels:
    app: project
    component: swarm-manager
spec:
  selector:
    app: project
    component: swarm-manager
  ports:
    - port: 8005
      targetPort: 8005
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: chronos
  labels:
    app: project
    component: chronos
spec:
  selector:
    app: project
    component: chronos
  ports:
    - port: 8006
      targetPort: 8006
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: project
    component: postgres
spec:
  selector:
    app: project
    component: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: project-ingress
  labels:
    app: project
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - api.project.example.com
      secretName: project-tls
  rules:
    - host: api.project.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-gateway
                port:
                  number: 80
